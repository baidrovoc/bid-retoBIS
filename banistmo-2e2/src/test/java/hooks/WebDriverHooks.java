package hooks;

import io.cucumber.java.Before;

public class WebDriverHooks {
    
    @Before
    public void setupWebDriver() {
        // Serenity maneja el driver automáticamente con autodownload
    }
}